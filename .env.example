# ──────────────────────────────────────────────
# US Broker — Robinhood
# ──────────────────────────────────────────────
RH_USERNAME=your_username
RH_PASSWORD=your_password
# RH_MFA_CODE is the base32 TOTP SECRET from Robinhood's 2FA setup page
# (e.g. "JBSWY3DPEHPK3PXP"), NOT a 6-digit code.
# Leave blank if MFA is not enabled.
RH_MFA_CODE=

# ──────────────────────────────────────────────
# India Broker — Zerodha / Kite Connect
# ──────────────────────────────────────────────
KITE_API_KEY=your_api_key
KITE_ACCESS_TOKEN=your_access_token

# ──────────────────────────────────────────────
# India Broker — ICICI Direct / Breeze
# ──────────────────────────────────────────────
ICICI_API_KEY=your_api_key
ICICI_SECRET_KEY=your_secret_key
ICICI_SESSION_TOKEN=your_session_token

# ──────────────────────────────────────────────
# AI Provider (openai, azure_openai, or gemini)
# ──────────────────────────────────────────────
AI_PROVIDER=azure_openai

# Azure OpenAI Credentials
AZURE_OPENAI_API_KEY=your_azure_api_key
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_VERSION=2023-05-15
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# Standard OpenAI (Optional)
OPENAI_API_KEY=sk-...

# Google Gemini (Optional)
GEMINI_API_KEY=AI...

# ──────────────────────────────────────────────
# Trading Mode
# ──────────────────────────────────────────────
# paper = simulated trades, live = real money
TRADING_MODE=paper

# ──────────────────────────────────────────────
# Broker Preferences (per market region)
# ──────────────────────────────────────────────
# US market: "robinhood"
US_PREFERRED_BROKER=robinhood
# India market: "zerodha" or "icici"
INDIA_PREFERRED_BROKER=zerodha
# India fallback if preferred is unavailable (optional)
INDIA_FALLBACK_BROKER=icici

# ──────────────────────────────────────────────
# Per-Region Capital Limits
# ──────────────────────────────────────────────
# Maximum total capital the agent can deploy in US market (USD)
US_MAX_CAPITAL=500.00
# Optional: max capital per single trade in US (USD)
# Leave blank to use default (20% of US_MAX_CAPITAL)
US_MAX_PER_TRADE=

# Maximum total capital the agent can deploy in India market (INR)
INDIA_MAX_CAPITAL=12000.00
# Optional: max capital per single trade in India (INR)
# Leave blank to use default (20% of INDIA_MAX_CAPITAL)
INDIA_MAX_PER_TRADE=

# ──────────────────────────────────────────────
# Global Risk Settings
# ──────────────────────────────────────────────
# Legacy global capital limit (regional limits above take precedence)
MAX_CAPITAL=1000.00
# Max percentage of regional capital to risk per trade (e.g. 0.02 = 2%)
MAX_RISK_PER_TRADE=0.02

# ──────────────────────────────────────────────
# Per-Region Watchlists (comma-separated tickers)
# ──────────────────────────────────────────────
# US stocks/ETFs to monitor
US_WATCHLIST=AAPL,TSLA,SPY,QQQ,MSFT
# Indian stocks to monitor (.NS suffix auto-added if missing)
INDIA_WATCHLIST=RELIANCE.NS,TCS.NS,INFY.NS,HDFCBANK.NS,TATASTEEL.NS
